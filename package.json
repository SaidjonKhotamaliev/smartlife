{
  "name": "smartlife",
  "version": "1.0.0",
  "description": "this is smartlife project",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "train": "nodemon --exec ts-node train.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steve",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1",
    "mongodb": "^3.2.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
